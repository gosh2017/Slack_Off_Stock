# 项目目标

请用Python编写一个桌面GUI程序，用于实时查询并显示A股股票的行情数据。

# 核心功能需求

1.  **股票代码输入**：程序启动后，提供一个输入框，让用户能输入A股股票代码（例如：`600000`、`000001`）。
2.  **手动查询**：提供一个"查询"按钮，点击后立即获取并显示该股票的最新数据。
3.  **自动刷新**：程序能按照用户设定的时间间隔（默认为60秒/1分钟）自动刷新数据。
4.  **数据显示**：在界面上清晰、突出地显示以下信息：
    *   股票名称
    *   最新价（现价）
    *   涨跌幅（百分比）
    *   （加分项）根据涨跌，将价格或百分比显示为红色（涨）或绿色（跌）。
5.  **程序控制**：提供"开始自动刷新"和"停止自动刷新"的按钮。

# 技术实现要求

1.  **编程语言**：Python 3。
2.  **图形界面 (GUI)**：使用Python内置的 `tkinter` 库，确保无需安装额外GUI框架即可运行[reference:3][reference:4]。
3.  **数据获取**：使用 `requests` 库调用免费的A股行情API。请优先选择稳定、无需申请API Key的公共接口[reference:5][reference:6]。
    *   **推荐数据源**：新浪财经接口 (`https://hq.sinajs.cn/list=`)[reference:7] 或 `akshare` 库。
    *   请确保代码能正确解析API返回的数据，计算出涨跌幅。
4.  **多线程**：数据获取和刷新操作必须在**后台线程**中执行，以防止界面卡顿[reference:9]。
5.  **异常处理**：程序需包含健壮的错误处理（如网络请求失败、股票代码无效等），并以友好的方式（如弹窗或状态栏提示）通知用户。
6.  **代码规范**：生成的代码应包含清晰的中文注释，结构合理，易于理解和修改。

# 使用与操作说明

1.  程序启动后，用户可在输入框中输入股票代码。
2.  点击"查询"按钮可立即查看结果。
3.  点击"开始刷新"按钮，程序将每隔60秒自动更新数据。
4.  点击"停止刷新"按钮可暂停自动更新。
5.  程序窗口应大小适中，各元素布局合理。

# 额外约束（可选，根据需要添加）

- ✅ **窗口置顶** — 已实现：提供"置顶"复选框，勾选后主窗口始终显示在最前面[reference:12]。
- ✅ **同时监控多只股票** — 已实现：主界面以列表形式同时监控多只股票，支持加入/移除，点击"开始刷新"按间隔自动刷新所有监控股票。监控列表支持双击查询、右键打开K线窗口和移除[reference:13]。
- ✅ **如何安装和运行** — 已提供：见 README.md"安装依赖"和"运行程序"章节。

# 功能变更记录

| 日期 | 变更内容 |
|------|----------|
| 初始 | 基础查询功能：输入代码 → 查询 → 显示行情 |
| 迭代1 | 新增名称搜索、查询历史、悬浮窗、窗口置顶 |
| 迭代2 | 新增 K线图功能（日K/周K/月K + 均线 + 成交量） |
| 迭代3 | 新增多股票监控列表（加入监控、移除、自动刷新所有监控股票） |
| 迭代4 | K线图从底部标签页迁移至独立窗口，可单例打开 |
| 迭代5 | 监控列表移至界面主体区域，替代原行情数据表格 |
| 迭代6 | "开始刷新"改为刷新监控列表中所有股票（不再依赖输入框代码） |
